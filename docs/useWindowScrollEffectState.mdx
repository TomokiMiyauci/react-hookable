# useWindowScrollEffectState

Reactive `window` scroll state

## Usage

```tsx
import { useWindowScrollEffectState } from 'react-hookable'
const [{ scrollX, scrollY }] = useWindowScrollEffectState()
```
