# useConditionalEffect

`useEffect` with conditional function

## Usage

```tsx
import { useConditionalEffect } from 'react-hookable'
useConditionalEffect(effect, deps, (...deps) => boolean | undefined)
```
