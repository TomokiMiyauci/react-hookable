# useIntervalEffect

Effect for `setInterval` that clear automatically when unmount

## Usage

```tsx
import { useIntervalEffect } from 'react-hookable'
useIntervalEffect({
  callback: () => {
    // setInterfal
  },
  ms: 1000 // ms
})
```
