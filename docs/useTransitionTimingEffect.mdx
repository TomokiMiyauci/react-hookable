# useTransitionTimingEffect

Effect for transition timing

## Usage

```tsx
import { useTransitionTimingEffect } from 'react-hookable'
useTransitionTimingEffect({
  target: ref
  onBeforeEnter,
  onEnter,
  onAfterEnter,
  onBeforeLeave,
  onLeave,
  onAfterLeave,
}, deps, condition)
```
