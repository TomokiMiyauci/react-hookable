# useTouchEffect

[`TouchEvents`](https://developer.mozilla.org/en-US/docs/Web/API/TouchEvent) effect

## Usage

```tsx
import { useTouchEffect } from 'react-hookable'
useTouchEffect({
  target: window,
  onTouchStart: (ev) => {
    // onTouchStart
  },
  []
})
```
