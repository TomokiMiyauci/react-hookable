# useAnimationFrameEffect

Effect for `requestAnimationFrame` that clear automatically when unmount

## Usage

```tsx
import { useAnimationFrameEffect } from 'react-hookable'
useAnimationFrameEffect({
  callback: () => {
    // requestAnimationFrame
  }
})
```
