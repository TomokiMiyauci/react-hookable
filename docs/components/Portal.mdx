# Portal

Portal is used to transport any component or element to the end of `document.body`

## Usage

```tsx
import { Portal } from 'react-hookable'

render(
  <Portal>
    <Component />
  </Portal>
)``
```
