# useClickOutsideEffect

Effect for click outside of target

## Usage

```tsx
import { useClickOutsideEffect } from 'react-hookable'
useClickOutsideEffect(
  {
    target: ref,
    onClickOutside: () => {
      // call on click outside
    }
  },
  deps,
  condition
)
```
